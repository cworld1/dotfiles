alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."

alias :q exit
alias :Q exit
alias cls clear
alias where whereis
alias rmf 'rm -rf'
alias mkdir 'mkdir -v -p'
alias wget 'wget --hsts-file="$XDG_DATA_HOME/wget-hsts"'

# User
alias clean~ xdg-ninja
alias osfetch fastfetch
alias gitfetch onefetch

# Customize
# alias open explorer # file explorer
# Proxy (for macOS)
alias pboard 'open -a Safari "http://127.0.0.1:9090/ui/#/"'
alias pedit 'nvim ~/.config/webserver/gfwlist.pac'
